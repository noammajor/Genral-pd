# Training settings (mirrors ConStruct/configs/train/train_default.yaml)
n_epochs: 1000
batch_size: 512
lr: 0.0002
clip_grad: null          # float, null to disable
save_model: True
num_workers: 0
progress_bar: False
weight_decay: 1e-12
optimizer: adamw
seed: 0
