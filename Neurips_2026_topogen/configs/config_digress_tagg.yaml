defaults:
    - _self_
    - general : general_default
    - dataset : planar
    - model   : digress
    - train   : train_default
    - optional digress_tagg_experiment: ???

hydra:
  job:
    chdir: True
  run:
    dir: runs/digress_tagg/${dataset.name}/marginal/layers=${model.n_layers}
